{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 1,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "tempo", "uid": "tempo" },
      "gridPos": { "h": 18, "w": 24, "x": 0, "y": 0 },
      "id": 1,
      "options": {},
      "targets": [
        {
          "query": "{ resource.service.name = `periplus-backend` && name = `agent.run` }",
          "queryType": "traceql",
          "refId": "A"
        }
      ],
      "title": "Agent runs and traces",
      "type": "traces"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "ms" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 18 },
      "id": 2,
      "options": {
        "legend": { "displayMode": "list", "placement": "bottom" },
        "tooltip": { "mode": "multi" }
      },
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(periplus_agent_run_duration_milliseconds_bucket[5m])) by (le, runtime, mode, status))",
          "legendFormat": "p95 {{runtime}} / {{mode}} / {{status}}",
          "refId": "A"
        }
      ],
      "title": "Run p95 by runtime and status",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "grafana", "uid": "-- Grafana --" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 18 },
      "id": 3,
      "options": {
        "content": "Use Tempo TraceQL to filter by `session.id`, `periplus.session.id_hash`, or `periplus.agent.run_id_hash`. Select a run above to inspect the complete message → runtime → MCP → command waterfall.",
        "mode": "markdown"
      },
      "title": "Session and run lookup",
      "type": "text"
    }
  ],
  "refresh": "15s",
  "schemaVersion": 39,
  "tags": ["periplus", "agent", "trace"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "Periplus Agent Runs",
  "uid": "periplus-agent-runs",
  "version": 1
}
